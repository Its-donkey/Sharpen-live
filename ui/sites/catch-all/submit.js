document.addEventListener("DOMContentLoaded", () => {
  if (window && window.console) {
    console.warn("[catch-all] Serving fallback site; submissions/admin are disabled until the target site loads.");
  }
});
