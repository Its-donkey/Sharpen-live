document.addEventListener("DOMContentLoaded", () => {
  if (window && window.console) {
    console.warn("[default-site] Serving fallback site; submissions/admin are disabled until the target site loads.");
  }
});
